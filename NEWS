Changes in movMF version 0.0-2
  
  o Function A() modified to have now three methods ("PCF" - Perron
    continued fraction, "GCF" - Gauss continued fraction, "RH" - using
    the generalized hypergeometric series). By default the "PCF"
    method is used.

Changes in movMF version 0.0-1

  o The random initializations for multiple runs are now determined
    within the loop separately for each run. 


First version released on CRAN: 0.0-0

